# Open New Terminal of Current File
As the title
## Usage
Run command `Open New Terminal of Current File` when you open a file, a new terminal will be created in the folder containing your opened file.